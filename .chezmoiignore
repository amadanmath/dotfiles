# Obviously
/.config/chezmoi

# Windows-only files: only deploy on Windows
{{ if ne .chezmoi.os "windows" -}}
Documents/
run_01_install-windows.ps1
{{- end }}
# On Windows, skip the POSIX install script (use the PowerShell one instead)
{{ if eq .chezmoi.os "windows" -}}
run_01_install-dependencies.sh
{{- end }}

# Repository README
/README.md

# Claude Code guidance
/CLAUDE.md

# Stuff for work, stuff not for work, transient stuff
/work
/xwork
/tmp

# Individual app/lang folders
/go
/obsidian

# Local stuff
/.local
/.gitconfig_local

# Secrets (never commit)
/.aws/credentials.json
/.kaggle
.envrc
/.ssh

# Shell stuff not managed here
/.mkshrc
/.zlogin

# Shared public key stuff by Mozilla and others
/.pki

# X credentials
/.Xauthority

# wget stuff
/.wget-hsts

# Audio (PulseAudio)
/.config/pulse

# Various caches and generated data
/.cache
/.mypy_cache
/.luarocks
/.zcompdump
/.zsh_sessions
/.config/nvim/lazy-lock.json

# Various history files
/.bash_history
/.zsh_history
/.python_history
/.lesshst
/.irb-history
/.sqlite_history
/.viminfo

# Various stores / package managers / environments
/.rvm
/.npm
/.venv
/.docker

# Various settings (machine-local state)
/.config/configstore
/.config/dconf
/.config/obsidian

# NVIDIA stuff
/.nv

# macOS / system junk
.DS_Store
.Trash
/.CFUserTextEncoding

# Windows system / generated files
/AppData
/scoop
NTUSER.DAT*
Desktop.ini

# Various housecleaning flags
/.motd_shown
/.sudo_as_admin_successful
